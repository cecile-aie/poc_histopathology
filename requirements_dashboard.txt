# Core numérique
numpy
pandas
tqdm

# Deep learning (CPU - pour EC2)
# Note: Pour GPU, remplacer par les versions CUDA standard
# Installation via pip avec index URL (à installer séparément ou via Dockerfile)
# torch==2.3.0+cpu --index-url https://download.pytorch.org/whl/cpu
# torchvision==0.18.0+cpu --index-url https://download.pytorch.org/whl/cpu
# torchaudio==2.3.0+cpu --index-url https://download.pytorch.org/whl/cpu
# Pour l'instant, on utilise l'image pytorch/pytorch:2.3.0-cpu qui inclut déjà torch
timm

# ML classique / métriques
scikit-learn

# Visualisation
matplotlib
seaborn

# Images
Pillow
opencv-python-headless
scipy

# FID / LPIPS
torch-fidelity
lpips

# Stack diffusion / PixCell / UNI2-h
diffusers
transformers
accelerate
safetensors
einops

# Histo
torch-staintools

# App / cloud
streamlit
boto3
s3fs